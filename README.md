# Instructions
Copy over and modify your configuration, including hardware-configuratin.nix

# Usage
nixos-rebuild switch --flake [PATH]#[CONFIGURATION NAME]

# Notes
Includes a nh configured to have the flake file in ~/.nixos by default.
